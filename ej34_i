#include<stdlib.h>
#include<stdio.h>
#include<stdbool.h>

 int main(int argc ,char * argv[]){
	int x = atoi(argv[1]);
	int y = atoi(argv[2]);
	int z = atoi(argv[3]);
	bool res;
        res = x<y&&y<z;
	res = res || x>y&&y>z;
	printf("%s\n",res ? "verdadero":"falso");
	return 0;

}
